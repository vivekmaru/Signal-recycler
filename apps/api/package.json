{
  "name": "@signal-recycler/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@openai/codex-sdk": "^0.125.0",
    "@signal-recycler/shared": "workspace:*",
    "fastify": "^5.6.2",
    "openai": "^6.11.0",
    "undici": "^7.16.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
