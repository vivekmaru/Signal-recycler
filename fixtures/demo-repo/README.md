# Demo Fixture

This repository is intentionally tiny. Its project convention is that package scripts must be run with `pnpm`, not `npm`.

Signal Recycler's first demo run should surface or describe the wrong `npm` path. After the user approves the generated rule, the second run should show Codex receiving the rule before acting.
